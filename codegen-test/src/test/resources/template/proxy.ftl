${copyright}
package ${proxyPackage};

import ${modelPackage}.${entityName};
import com.letv.portal.proxy.IBaseProxy;

/**
* ${proxyDescription}
* @author ${author} .
* @since ${(.now?string(sice))!} .
* @version ${version} .
*/
public interface ${proxyName} extends IBaseProxy<${entityName}> {
}
